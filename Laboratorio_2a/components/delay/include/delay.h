#ifndef DELAY_H
#define DELAY_H

#include <stdint.h>

// En esta biblioteca solo se declararon las funciones que seran utilizadas en delay.c
void delay_ms(uint32_t ms);
void delay_s(uint32_t s);

#endif
